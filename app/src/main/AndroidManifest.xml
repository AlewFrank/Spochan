<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.spochansecondversion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditMyProfileActivity"></activity>
        <activity android:name=".RatingActivity" />
        <activity android:name=".MyProfileActivity" />
        <activity android:name=".CompetitionsActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".TestTabNews" />
        <activity android:name=".TabCompetitions" />
        <activity android:name=".TabNews" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>